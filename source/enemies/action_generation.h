#pragma once

#include "id_to_enemy.h"

extern void reset_usage(vector<possibleActions>& actions);
extern Action rand_get(vector<possibleActions>& actions);
extern Action rotate_get(vector<possibleActions>& actions);
